#[derive(Copy, Clone)]
pub struct IntegerConst {
	pub bytes: u32,
}

#[derive(Copy, Clone)]
pub struct FloatConst {
	pub bytes: u32,
}

#[derive(Copy, Clone)]
pub struct LongConst {
	pub bytes: u64,
}

#[derive(Copy, Clone)]
pub struct DoubleConst {
	pub bytes: u64,
}