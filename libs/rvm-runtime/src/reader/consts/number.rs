#[derive(Copy, Clone)]
pub struct IntegerConst {
	pub bytes: i32,
}

#[derive(Copy, Clone)]
pub struct FloatConst {
	pub bytes: f32,
}

#[derive(Copy, Clone)]
pub struct LongConst {
	pub bytes: i64,
}

#[derive(Copy, Clone)]
pub struct DoubleConst {
	pub bytes: f64,
}